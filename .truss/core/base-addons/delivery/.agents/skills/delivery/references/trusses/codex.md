# Codex CLI

Orca agent ID: `codex`.
Permission default: `--dangerously-bypass-approvals-and-sandbox`.
Forbidden headless form: `codex exec`.

`worker-start --model` supports this agent.
`--effort` requires `--model`; neither combines with `--terminal`.
Shared readiness and retry rules remain in `../../SKILL.md`.
