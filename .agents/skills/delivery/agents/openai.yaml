interface:
  display_name: "Delivery"
  short_description: "Deliver a change through an approved contract, implementation, review, and release"
  default_prompt: "Use $delivery to take the requested change through design approval, implementation, independent review, and release."
policy:
  allow_implicit_invocation: false
