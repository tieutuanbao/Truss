interface:
  display_name: "Delivery"
  short_description: "Deliver a change through an approved contract, seven-role implementation, independent acceptance, and release"
  default_prompt: "Use $delivery to take the requested change through design approval, seven-role implementation, independent tester-debugger acceptance, and release."
policy:
  allow_implicit_invocation: false
